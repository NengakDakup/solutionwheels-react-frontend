import React, { Component } from 'react'

class updateButton extends Component {
    render(){
        return (
            <button>Load New Questions</button>
        )
    }
}

export default updateButton;